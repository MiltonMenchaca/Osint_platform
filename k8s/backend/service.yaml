apiVersion: v1
kind: Service
metadata:
  name: osint-backend
  namespace: osint-platform
  labels:
    app: osint-backend
spec:
  selector:
    app: osint-backend
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
